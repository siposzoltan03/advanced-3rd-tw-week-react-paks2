import React from "react"

export const City = () => {
    const name = 0;
    const score = 1;
    return(
        <div className="city">
           {/* <p>{cityName}</p> */}
        </div>
    )
}

export default City;